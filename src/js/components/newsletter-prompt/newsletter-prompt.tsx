import React, { ReactElement } from "react";
import "./newsletter-prompt.scss";

const NewsletterPrompt = () => {
  return (
    <a href="/newsletter" className="newsletter-frontpage-link">
      <span>Join our </span>
      <span>newsletter</span>
    </a>
  );
};

export default NewsletterPrompt;
